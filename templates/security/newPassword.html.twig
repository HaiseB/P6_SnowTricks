{% extends 'base.html.twig' %}

{% block title %}
    Changement de mot de passe
{% endblock %}

{% block body %}
    <h1>Changement de mot de passe</h1>

    <fieldset>
        {{ form_start(form) }}
        <div class="form-group">
            {{ form_errors(form) }}
        </div>

        <div class="form-group">
            {{ form_row(form.password) }}
        </div>

        <button type="submit" class="btn btn-success btn-block">Valider le changement</button>
        {{ form_end(form) }}
    </fieldset>
{% endblock %}
