{% extends 'base.html.twig' %}

{% block body %}
    <div id="header" class="text-center">
        <h1>Snowtricks</h1>
        <h3>Pour les passionnés de snowboard, un réseau social collaboratif pour faire connaître ce sport auprès du grand public et aider à l'apprentissage des figures</h3>
        <a class="js-scrollTo" href="#tricks"><i class="fas fa-arrow-to-bottom"></i></a>
        <a class="js-scrollTo" href="#header"><i class="fas fa-arrow-to-top"></i></a>
    </div>
    <section class="text-center">
        <div id="tricks">
            <p>Chargement des tricks <div class="loader"></div></p>
        </div>
    </section>
    <a class="card text-white bg-primary mb-3" id="load_more_button"  href="#tricks">Charger plus</a>
    {#  % for trick in tricks %}
        <div>
            {{ trick.name }} |
            <a href="{{ path('app_trick_show', {id : trick.id}) }}" >Aller au trick</a> |
            {{ trick.tag.name  }}
        </div>
    {% endfor %}#}

{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('trick_homepage') }}
{% endblock %}